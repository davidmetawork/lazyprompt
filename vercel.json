{
  "version": 2,
  "buildCommand": "npm install && cd packages/database && npx prisma generate && cd ../../apps/marketplace && npm install && npm run build",
  "outputDirectory": "apps/marketplace/.next",
  "framework": "nextjs",
  "installCommand": "npm install"
} 